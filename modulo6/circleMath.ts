namespace MinhaMatematica {

    export namespace Circulo {
        const PI = 3.1415;

        export function calculaCircunferencia(diametro: number) {
            return diametro * PI;
        }
    }
}