let idade = 35;
// idade = "35";

function meControle(isTrue: boolean, algoMais: boolean) {
    let resultado: number;
    if (isTrue) {
        resultado = 12;
    }
    resultado = 33;
    return resultado;
}